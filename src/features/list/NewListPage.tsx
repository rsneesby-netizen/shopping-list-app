import { ListPage } from './ListPage'

/**
 * Redesign scaffold entrypoint.
 * Keep parity by default while iterating on the new UI behind VITE_ENABLE_NEW_UI.
 */
export function NewListPage() {
  return <ListPage />
}
